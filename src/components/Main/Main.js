import React from 'react';

const Main = ({ children }) => {
  return (
    <div className="d-lg-flex justify-content-lg-center align-items-lg-center">
      {children}
    </div>
  );
};

export default Main;
